# Render.com 배포 설정
# Dashboard > New > Blueprint 으로 이 파일 선택해 자동 배포 가능
services:
  - type: web
    name: stock-trader
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webapp:app --bind 0.0.0.0:$PORT --timeout 120
